import mongoose from 'mongoose';
import slack from 'slack';
import Token from '../app/tokens';

export default function bot() {
  // const botTokenPromise = Token.findOne({ access_token: data.access_token }).exec();
};
